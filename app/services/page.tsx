import Link from "next/link"; import Reveal from "@/components/Reveal"; import {services} from "@/lib/services";
export const metadata={title:'Physiotherapy Services'};
export default function Services(){return <><section className="page-hero"><div className="eyebrow">WHAT WE HELP WITH</div><h1>Physiotherapy that meets you where you are.</h1><p>From everyday pain to sports rehabilitation and post-surgical recovery, each plan starts with assessment and builds toward useful function.</p></section><section className="service-grid">{Object.entries(services).map(([slug,s],i)=><Reveal key={slug} delay={(i%4)*60} variant="scale"><Link href={`/services/${slug}`}><article className="service-card service-card-image"><div className="service-card-media"><img src={s.image} alt={`${s.title} physiotherapy`} loading="lazy"/></div><div className="service-card-body"><div className="num">{s.category}</div><h3>{s.title}</h3><p>{s.description}</p><span className="arrow">Explore treatment →</span></div></article></Link></Reveal>)}</section></>}
