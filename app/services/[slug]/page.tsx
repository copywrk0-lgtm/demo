import Link from "next/link"; import {notFound} from "next/navigation"; import Reveal from "@/components/Reveal"; import {services} from "@/lib/services";
export function generateStaticParams(){return Object.keys(services).map(slug=>({slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=services[slug as keyof typeof services];return s?{title:s.title,description:s.description}:{}}
export default async function ServicePage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=services[slug as keyof typeof services];if(!s)notFound();return <><section className="page-hero"><div className="eyebrow">{s.category}</div><h1>{s.title}</h1><p>{s.description}</p></section>
<Reveal><section className="service-detail"><article className="detail-card"><div className="eyebrow">THE PLAN</div><h2>A practical route from where you are to where you want to be.</h2><p className="lead">{s.lead}</p><h3>What your plan may include</h3><ul>{s.bullets.map(x=><li key={x}>{x}</li>)}</ul><h3>When an assessment may help</h3><p>{s.fit}</p><Link className="cta" href="/contact">Book an assessment →</Link></article><div className="detail-photo"><img src={s.image} alt={`${s.title} physiotherapy care`}/></div></section></Reveal>
<Reveal><section className="section"><div className="process-strip"><div><span>01</span><strong>Assess</strong><p>Understand the problem and your goals.</p></div><div><span>02</span><strong>Plan</strong><p>Choose the right mix of treatment and exercise.</p></div><div><span>03</span><strong>Progress</strong><p>Build capacity and adjust as you improve.</p></div><div><span>04</span><strong>Return</strong><p>Get back to the activities that matter.</p></div></div></section></Reveal>
<Reveal><section className="section"><div className="closing-cta"><div><div className="eyebrow">READY WHEN YOU ARE</div><h2>Let’s work out the next useful step.</h2><p>You do not need to have everything figured out before booking an assessment.</p></div><Link className="cta light" href="/contact">Contact the clinic →</Link></div></section></Reveal></> }